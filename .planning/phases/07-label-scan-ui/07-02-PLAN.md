---
phase: 07-label-scan-ui
plan: 02
type: execute
depends_on: ["07-01"]
files_modified: [src/components/Scanner.tsx, src/components/LabelScanner.tsx]
---

<objective>
Add mode switching UI within Scanner and LabelScanner components.

Purpose: Allow users to switch between barcode and label scan modes without returning to mode selection.
Output: Both scanner components have a mode switch button that flips to the other mode.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-label-scan-ui/07-01-SUMMARY.md
@.planning/phases/07-label-scan-ui/07-CONTEXT.md

# Key files:
@src/components/Scanner.tsx
@src/components/LabelScanner.tsx
@src/App.tsx

**From 07-CONTEXT.md:**
- Mode switch available at bottom of each flow to flip to the other mode
- Smooth switching without going back to mode selection
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add mode switch to Scanner component</name>
  <files>src/components/Scanner.tsx</files>
  <action>
Add `onModeSwitch` prop to Scanner and render mode switch button.

Props update:
```typescript
interface ScannerProps {
  // ... existing props
  onModeSwitch: () => void;  // New
}
```

UI placement: In bottom bar, above or next to the Pause button
- Small text link or subtle button: "Switch to Label Scan →"
- Style: text-gray-400 hover:text-white, subtle so it doesn't compete with Pause button

When clicked: Call onModeSwitch() which triggers App.tsx to switch mode state.

Note: Switching modes should disconnect from current barcode session (App.tsx handles this via existing disconnect flow).
  </action>
  <verify>tsc --noEmit passes, mode switch button visible in Scanner</verify>
  <done>Scanner.tsx accepts onModeSwitch prop and renders switch button in bottom bar</done>
</task>

<task type="auto">
  <name>Task 2: Add mode switch to LabelScanner component</name>
  <files>src/components/LabelScanner.tsx</files>
  <action>
Wire up the onModeSwitch prop added in Plan 01 Task 1.

UI placement: In bottom bar (pair code screen and camera screen)
- Text link: "Switch to Barcode Scan →"
- Same subtle styling as Scanner: text-gray-400 hover:text-white
- Position: Below capture button (camera screen) or below Connect button (pair code screen)

When clicked: Call onModeSwitch() which triggers App.tsx to switch to barcode mode.

Switching should disconnect from label scan session if active (call disconnect() before switching).
  </action>
  <verify>tsc --noEmit passes, mode switch visible in LabelScanner</verify>
  <done>LabelScanner.tsx shows mode switch button that triggers onModeSwitch</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Mode switching between barcode and label scan modes</what-built>
  <how-to-verify>
    1. Run: npm run dev
    2. Visit: http://localhost:5173
    3. Select "Scan Barcode" mode
    4. In Scanner view, find "Switch to Label Scan" link at bottom
    5. Click it — should switch to Label Scan pair code entry
    6. Find "Switch to Barcode Scan" link
    7. Click it — should switch back to Barcode pair code entry
    8. Test: Switching preserves nothing (clean slate each time)
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `tsc --noEmit` passes
- [ ] `npm run build` succeeds
- [ ] Mode switch works in both directions
- [ ] Switching clears previous session state
</verification>

<success_criteria>

- Scanner has working mode switch to label scan
- LabelScanner has working mode switch to barcode scan
- Phase 7 complete — Label Scan UI fully implemented
- v2.0 milestone complete
</success_criteria>

<output>
After completion, create `.planning/phases/07-label-scan-ui/07-02-SUMMARY.md`

Final summary should note v2.0 Label Scan OCR milestone is complete.
</output>
