---
phase: 04-scan-submit-handler
plan: 01
type: execute
---

<objective>
Fix phone scanner WebSocket handler to properly handle server responses.

Purpose: The scan.submit backend handler is already fully implemented. The phone needs to recognize the `scan.confirmed` response type (currently expects `scan.received`) and properly display server errors.

Output: Phone scanner handles server responses correctly - shows errors when server reports failures, maintains optimistic feedback on success.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-scan-submit-handler/04-RESEARCH.md

**Research finding:** The entire backend is implemented (realtime-gateway:145-211, order-service:269-374). Only the phone's response handler needs updating.

**Current state:**
- Phone sends `scan.submit` → Works ✅
- Gateway forwards to order-service → Works ✅
- Order-service stores and publishes → Works ✅
- Desktop receives via Redis broadcast → Works ✅
- Phone handles `scan.confirmed` → ❌ Expects `scan.received`
- Phone handles `scan.error` → ❌ Not implemented

**Gateway responses:**
- Success: `{ type: 'scan.confirmed', data: { id, barcode, scanned_at } }`
- Error: `{ type: 'scan.error', error: 'reason string' }`

**Key files:**
@src/hooks/useScannerSession.ts - WebSocket message handler (lines 63-87)
@src/components/Scanner.tsx - UI with ScanFeedback component
@src/types/index.ts - WebSocketMessage type
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update WebSocket message handler for server responses</name>
  <files>src/hooks/useScannerSession.ts, src/types/index.ts</files>
  <action>
1. In useScannerSession.ts ws.onmessage handler (lines 63-87), update the switch statement:
   - Change `case 'scan.received'` to `case 'scan.confirmed'`
   - Add new state for scan confirmation tracking if needed
   - Add `case 'scan.error'` handler that sets error state and triggers error feedback

2. In types/index.ts, add optional `data` field to WebSocketMessage for scan.confirmed response:
   ```typescript
   export interface WebSocketMessage {
     type: string;
     session_id?: string;
     barcode?: string;
     scanned_at?: string;
     error?: string;
     data?: { id: number; barcode: string; scanned_at: string };
   }
   ```

3. Add a lastScanError state to useScannerSession hook to track scan-specific errors (separate from connection errors)

Keep optimistic feedback (show checkmark immediately on scan) - the error case is for when server explicitly rejects.
  </action>
  <verify>TypeScript compiles: `npm run build` succeeds</verify>
  <done>useScannerSession handles `scan.confirmed` and `scan.error` message types</done>
</task>

<task type="auto">
  <name>Task 2: Add scan error feedback to Scanner component</name>
  <files>src/components/Scanner.tsx, src/hooks/useScannerSession.ts</files>
  <action>
1. Export lastScanError from useScannerSession hook return value

2. In Scanner.tsx:
   - Accept lastScanError prop (or get from hook if Scanner uses the hook directly)
   - Show brief red error toast/overlay when lastScanError is set
   - Auto-dismiss after 2 seconds
   - Format: red banner at top with error message

Keep it simple - just show a brief toast that auto-dismisses. User can keep scanning.
  </action>
  <verify>npm run build succeeds, no TypeScript errors</verify>
  <done>Scanner displays error feedback when server rejects a scan</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `npm run build` succeeds without errors
- [ ] `npm run lint` passes (if configured)
- [ ] Types are correct - no TypeScript errors
- [ ] Code handles both success and error responses from server
</verification>

<success_criteria>

- useScannerSession properly handles `scan.confirmed` and `scan.error` responses
- Scanner component shows error feedback when server rejects a scan
- Optimistic success feedback still works (immediate checkmark on scan)
- Build passes with no errors
- Phase 4 complete
</success_criteria>

<output>
After completion, create `.planning/phases/04-scan-submit-handler/04-01-SUMMARY.md`:

# Phase 4 Plan 01: Scan Submit Handler Summary

**[One-liner summary of what shipped]**

## Accomplishments

- [Key outcome 1]
- [Key outcome 2]

## Files Created/Modified

- `path/to/file.ts` - Description

## Decisions Made

[Key decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Phase complete, ready for next milestone
</output>
